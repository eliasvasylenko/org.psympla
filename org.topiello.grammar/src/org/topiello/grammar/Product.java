package org.topiello.grammar;

public interface Product<T extends Variable> {
  /*
   * TODO this is the area to calculate which variables may be matched by the
   * product
   * 
   * TODO this could be the area to generate the behavior upon completing the
   * rule/terminal derived by the product.
   * 
   * TODO this could be the area to generate the behavior upon predicting from the
   * product, e.g. specializing predictions.
   */
}
